@* @inject ISessionService SessionService

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            ((SessionService)SessionService).OnAfterRender(firstRender);
        }
    }
}
 *@